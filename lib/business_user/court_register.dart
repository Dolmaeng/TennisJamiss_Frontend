import 'package:flutter/material.dart';

class court_register extends StatefulWidget{
  @override
  court_register_state createState() => court_register_state();
}

class court_register_state extends State<court_register> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
            child: SingleChildScrollView(

            )
        )
    );
  }
}